import PlaceholderPage from "@/components/admin/PlaceholderPage";

export const metadata = { title: "Marketing" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Marketing"
      description="Promotions, discount codes and campaigns for the delivery area."
    />
  );
}
